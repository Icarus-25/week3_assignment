### Example DATABASE_URL for PostgreSQL
# Format: postgres://USER:PASSWORD@HOST:PORT/DATABASE
DATABASE_URL=postgres://postgres:password@localhost:5432/week3_assignment_dev

# Put your real credentials in a `.env` file (not committed to source control)
